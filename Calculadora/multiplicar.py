class Multiplicar():
    def multiplicar(self, a, n):
        multipl = a * n
        for n in range (5):
            n = int(input("digite o numero (n)"))
            multn *= n
        return multipl
